node_modules
.next
out
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Output/scratch from the demo video pipeline (npm run demo:cinematic) —
# the pipeline scripts stay in the repo for whenever a demo is needed again,
# but the generated video/gif itself isn't currently linked from anywhere.
docs/demo/raw
docs/demo/work
docs/demo/*.mp4
docs/demo/*.gif

.vercel
.env*
